<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <uses-feature
        android:name="android.permission.CAMERA"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReZone"
        tools:targetApi="31">

        <activity
            android:name=".activities.auth.SplashActivity"
            android:theme="@style/AppTheme.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.auth.OnboardingActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />
        <activity
            android:name=".activities.auth.ChooseRegisterWayActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

        <activity
            android:name=".activities.auth.LoginPhoneActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />
        <activity
            android:name=".activities.auth.LoginEmailActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

        <activity
            android:name=".activities.auth.ForgotPasswordActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />
        <activity
            android:name=".activities.auth.OtpActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />
        <activity
            android:name=".activities.auth.ResetPasswordActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />
        <activity
            android:name=".activities.auth.PasswordChangedActivity"
            android:theme="@style/AppTheme"
            android:exported="false" />

    </application>

</manifest>